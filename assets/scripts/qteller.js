javascript: var x = document.getElementsByName('points');
var x_list = x[0].areas;
var outList = [];
 for (var i = 0; i < x_list.length; i++) {  var inStr=x_list[i].onmouseover.toString().split('\n');
      var out1=inStr[1].replace(/.*\('|[');]/gi,'');
    var tmp = out1.split(',');
    outList.push(tmp);} var data = 'Experiment,Paper,Tissue Prep,Expression,Lower Limit,Upper Limit\n';
outList.forEach(function (infoArray, index) { dataString = infoArray.join(',');
data += index < outList.length ? dataString + '\n' : dataString;
}); var pom = document.createElement('a');
var csvContent = data;
var blob = new Blob([csvContent], { type: 'text/csv;charset=utf-8;' });
var url = URL.createObjectURL(blob);
pom.href = url;
var y = document.getElementById('datacontainer');
var file_title = String(document.title).toLowerCase();
file_title += '.csv';
file_title = file_title.replace(' ', '_');
console.log(file_title);
pom.setAttribute('download', file_title);
document.body.appendChild(pom);
pom.click();
document.body.removeChild(pom);
console.log(file_title);
